---
import { getCollection } from "astro:content";
import PageLayout from "../../layouts/PageLayout.astro";

const entries = await getCollection("lp");
---

<PageLayout title="Landingpages">
  <head slot="head">
    <title>URSIO – Landingpages</title>
    <meta property="og:title" content="URSIO – Landingpages" />
    <meta property="og:type" content="website" />
  </head>

  <section class="lp-list">
    {entries.length === 0 ? (
      <p>Noch keine Landingpages vorhanden.</p>
    ) : (
      <ul>
        {entries.map((entry) => (
          <li>
            <a href={`/lp/${entry.slug}/`}>
              {entry.data.title}
            </a>
          </li>
        ))}
      </ul>
    )}
  </section>

  <style>
    .lp-list {
      max-width: 70ch;
      margin: 0 auto;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    li {
      margin-bottom: var(--space-md);
    }

    a {
      color: var(--color-accent);
      font-size: var(--text-lg);
      text-decoration: none;
    }

    a:hover {
      color: var(--color-accent-soft);
    }
  </style>
</PageLayout>